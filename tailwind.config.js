/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,ts}"], 
  // these are the templates we want to use
  //target all files with html and ts extension
  theme: {
    extend: {},
  },
  plugins: [],
}

